{
  "name": "world-cup-multisource-live-ticker-scoreboard-os",
  "version": "10.0.0",
  "type": "module",
  "private": true,
  "scripts": {
    "build:data": "node scripts/build-static-data.js",
    "build:pages": "npm run build:data && node scripts/build-pages.js",
    "dev": "node local-server.js",
    "start": "node local-server.js",
    "vercel:dev": "vercel dev",
    "deploy": "npm run build:pages && gh-pages -d dist --dotfiles --nojekyll"
  },
  "dependencies": {
    "cheerio": "^1.0.0"
  },
  "devDependencies": {
    "gh-pages": "^6.3.0",
    "vercel": "^34.0.0"
  }
}
